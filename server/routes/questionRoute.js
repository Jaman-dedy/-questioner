const express = require ("express");
const router = express.Router();

// from controller

questionCtrl = require("../controllers/questionController");

// question endpoints

module.exports = router;